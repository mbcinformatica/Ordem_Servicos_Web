@model Ordem_Servicos_Web.Models.Permissao

@{
    ViewData["Title"] = "Excluir Permissão";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<partial name="_MensagemToast" />

<modal-confirmacao id-modal="confirmarExclusao"
                   icone="fa fa-trash"
                   titulo="Excluir Permissão"
                   mensagem="Tem Certeza que Deseja Excluir Esta Permissão?"
                   classe-mensagem="mensagem-sucesso-modal"
                   action-controller="Permissoes"
                   action-name="Excluir"
                   id-registro="@Model.IdPermissao"
                   icone-botao-principal="fa fa-trash"
                   texto-botao-principal="Excluir"
                   classe-botao-principal="btn-excluir"
                   texto-botao-cancelar="Voltar"
                   classe-botao-secundario="btn-voltar"
                   action-controller-secundario="Permissoes"
                   action-name-secundario="Index">
</modal-confirmacao>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            bloquearMenu();
            var modalElement = document.getElementById("confirmarExclusao");
            if (modalElement) {
                var modal = new bootstrap.Modal(modalElement);
                modal.show();
            }
        });
    </script>
}