@model Ordem_Servicos_Web.Models.Marca

@{
    ViewData["Title"] = "Alterar Marca";
}

<h2 class="text-center mb-4">@ViewData["Title"]</h2>

<partial name="_MensagemToast" />

<div class="d-flex justify-content-center align-items-center ">
    <div class="card-form">
        <div class="card-body">
            <form asp-action="Alterar" method="post" id="formMarca"
                  data-campos-obrigatorios="Descricao">
                <input type="hidden" asp-for="IdMarca" />

                <!-- Descrição da Marca -->
                <div class="row mb-2 g-4">
                    <div class="col-auto">
                        <label asp-for="Descricao" class="label-destaque">Descrição da Marca</label>
                        <input asp-for="Descricao" class="form-control text-uppercase maiusculo descricaoMarca" id="Descricao" style="width:615px;" data-focus="true" />
                    </div>
                </div>

                <!-- Botões -->
                <div class="d-flex gap-2 mt-4">
                    <button type="submit" id="btnSalvar" class="btn-gradiente btn-gradiente-salvar">
                        <i class="fa fa-floppy-disk move-icone-esq"></i> Salvar Alterações
                    </button>
                    <a asp-action="Index" class="btn-gradiente btn-gradiente-cancelar">
                        <i class="fa fa-undo move-icone-esq"></i> Cancelar/Voltar
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            bloquearMenu();

            const descricao = document.getElementById("Descricao");
            const btnSalvar = document.getElementById("btnSalvar");
            const form = document.getElementById("formMarca");

            const camposObrigatorios = [
                "Descricao"
            ];

            if (form) {
                ValidacaoUtils.configurarValidacaoCampos(form, camposObrigatorios, "MARCAS");

                if (window.FormUtils && typeof FormUtils.enableEnterNavigation === 'function') {
                    FormUtils.enableEnterNavigation(form, { allowTextarea: true });
                }
            }

            if (btnSalvar && form) {
                btnSalvar.addEventListener("click", function(e) {
                    btnSalvar.focus();
                    const enterEvent = new KeyboardEvent("keydown", {
                        key: "Enter",
                        code: "Enter",
                        bubbles: true
                    });
                    btnSalvar.dispatchEvent(enterEvent);
                    form.requestSubmit();
                });

                btnSalvar.addEventListener("keydown", function(e) {

                    if (e.key === "Enter") {
                        form.requestSubmit();
                    }
                });
            }
        });
    </script>
}