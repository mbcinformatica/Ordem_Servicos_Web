@model Ordem_Servicos_Web.Models.Usuario

@{
    ViewData["Title"] = "Excluir Usuário";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<<<<<<< HEAD
<partial name="_MensagemToast" />

<modal-confirmacao id-modal="confirmarExclusao"
                   icone="fa fa-trash"
                   titulo="Excluir Usuário"
                   mensagem="Tem Certeza que Deseja Excluir Este Usuário?"
                   classe-mensagem="mensagem-sucesso-modal"
                   action-controller="Usuarios"
                   action-name="Excluir"
                   id-registro="@Model.IdUsuario"
                   icone-botao-principal="fa fa-trash"
                   texto-botao-principal="Excluir"
                   classe-botao-principal="btn-excluir"
                   texto-botao-cancelar="Voltar"
                   classe-botao-secundario="btn-voltar"
                   action-controller-secundario="Usuarios"
                   action-name-secundario="Index">
</modal-confirmacao>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            bloquearMenu();
            var modalElement = document.getElementById("confirmarExclusao");
            if (modalElement) {
                var modal = new bootstrap.Modal(modalElement);
                modal.show();
            }
        });
    </script>
=======
<div class="alert alert-danger text-center mt-3">
    <p class="fw-bold fs-5">
        Tem certeza que deseja excluir o usuário <strong>@Model.NomeUsuario</strong>?
    </p>
</div>

<form asp-action="Excluir" method="post" class="text-center mt-4">
    <input type="hidden" asp-for="IdUsuario" />

    <div class="d-flex justify-content-center gap-3">
        <button type="submit" class="btn btn-danger">
            <i class="fa fa-trash"></i> Excluir
        </button>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="fa fa-arrow-left"></i> Cancelar
        </a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
>>>>>>> 2ddd7e7f7bdc32e421f23a847a01c8ab48c6f1d7
}