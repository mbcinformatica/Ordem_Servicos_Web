@model dynamic

<div class="modal fade" id="@Model.IdModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered mensagem-texto">
        <div class="modal-content text-center @Model.ClasseMensagem">

            <!-- Cabeçalho -->
            <div class="modal-header d-flex justify-content-center align-items-center fw-bold fs-5">
                <i class="@Model.Icone me-2 move-icone-esq"></i>
                <span>@Model.Titulo</span>
            </div>


            <!-- Corpo -->
            <div class="modal-body text-center fw-bold fs-5">
                <span>@Html.Raw(Model.Mensagem)</span>
                <form asp-controller="@Model.ActionController" asp-action="@Model.ActionName" method="post" class="text-center mt-4">
                    @if (Model.IdRegistro != null)
                    {
                        <input type="hidden" name="Id" value="@Model.IdRegistro" />
                    }
                    <div class="d-flex justify-content-center gap-3">
                        <!-- Botão principal -->
                        <button type="submit" class="btn-gradiente btn-gradiente-excluir">
                            <i class="@Model.IconeBotaoPrincipal move-icone-esq"></i> @Model.TextoBotaoPrincipal
                        </button>

                        <!-- Botão secundário -->
                        @if (!string.IsNullOrEmpty(Model.ActionControllerSecundario) && !string.IsNullOrEmpty(Model.ActionNameSecundario))
                        {
                             <a asp-controller="@Model.ActionControllerSecundario" asp-action="@Model.ActionNameSecundario" class="btn-gradiente btn-gradiente-cancelar">
                                <i class="fa fa-undo move-icone-esq"></i> @Model.TextoBotaoCancelar
                            </a>
                        }
                        else
                        {
                            <button type="button" class="btn-gradiente btn-gradiente-cancelar" data-bs-dismiss="modal">
                                <i class="fa fa-undo move-icone-esq"></i> @Model.TextoBotaoCancelar
                            </button>
                        }
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>