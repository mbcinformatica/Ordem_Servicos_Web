@{
    var mensagem = TempData["Mensagem"]?.ToString();
<<<<<<< HEAD
    var tipo = TempData["MensagemTipo"]?.ToString()?.ToLower();
    var titulo = tipo switch { "erro" => "Erro", "aviso" => "Aviso", "informacao" => "Informação", _ => "Sucesso" };
    var icone = tipo switch { "erro" => "fa-times-circle", "aviso" => "fa-exclamation-circle", "informacao" => "fa-info-circle", _ => "fa-check-circle" };
    var classeMensagem = tipo switch { "erro" => "mensagem-erro-modal", "aviso" => "mensagem-aviso-modal", "informacao" => "mensagem-info-modal", _ => "mensagem-sucesso-modal" };
}

@if (!string.IsNullOrEmpty(mensagem))
{
    <div class="modal fade" id="mensagemModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content modal-content-tempdata text-center @classeMensagem">

                <!-- Cabeçalho -->
                <div class="modal-header d-flex justify-content-center align-items-center fw-bold fs-5">
                    <i class="fa @icone me-2"></i>
                    <span>@titulo</span>
                </div>

                <!-- Corpo -->
                <div class="modal-body text-center fw-bold fs-5">
                    <span class="mensagem-texto">@mensagem</span>
                    <div class="mt-4">
                        <button type="button" id="btnOk" class="btn-ok" data-bs-dismiss="modal">OK</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
=======
    var tipo = TempData["MensagemTipo"]?.ToString() ?? "sucesso";

    var titulo = tipo == "erro" ? "Erro" : tipo == "aviso" ? "Aviso" : "Sucesso";
    var cor = tipo == "erro" ? "bg-danger text-white"
             : tipo == "aviso" ? "bg-warning text-dark"
             : "bg-success text-white";
    var icone = tipo == "erro" ? "fa-times-circle"
              : tipo == "aviso" ? "fa-exclamation-circle"
              : "fa-check-circle";
}

<div class="modal fade" id="mensagemModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content text-center">
            <div class="modal-header bg-success text-white justify-content-center">
                <h5 class="modal-title" id="mensagemTitulo">
                    <i class="fa fa-check-circle me-2"></i> Mensagem
                </h5>
            </div>
            <div class="modal-body">
                <p class="fw-bold fs-5" id="mensagemTexto"></p>
            </div>
            <div class="modal-footer justify-content-center">
                <button type="button" class="btn btn-primary" data-bs-dismiss="modal">OK</button>
            </div>
        </div>
    </div>
</div>

@if (!string.IsNullOrEmpty(TempData["Mensagem"]?.ToString()))
{
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const tituloEl = document.getElementById("mensagemTitulo");
            const textoEl = document.getElementById("mensagemTexto");
            if (tituloEl && textoEl) {
                tituloEl.innerText = "@(TempData["MensagemTipo"] ?? "Sucesso")";
                textoEl.innerText = "@TempData["Mensagem"]";
            }
>>>>>>> 2ddd7e7f7bdc32e421f23a847a01c8ab48c6f1d7
            var modalElement = document.getElementById("mensagemModal");
            if (modalElement) {
                var modal = new bootstrap.Modal(modalElement);
                modal.show();
<<<<<<< HEAD

                // Foca no botão OK quando o modal abrir
                modalElement.addEventListener("shown.bs.modal", function () {
                    var btnOk = document.getElementById("btnOk");
                    if (btnOk) btnOk.focus();
                });

                // Fade-out ao clicar em OK
                const btnOk = modalElement.querySelector(".btn-ok");
                btnOk.addEventListener("click", function () {
                    const content = modalElement.querySelector(".modal-content");
                    if (content) {
                        content.classList.add("fade-out");
                    }
                    setTimeout(() => modal.hide(), 2000);
                });
=======
>>>>>>> 2ddd7e7f7bdc32e421f23a847a01c8ab48c6f1d7
            }
        });
    </script>
}